html-math-method:
  method: mathml
include-in-header:
  - ${.}/linewrap.tex
metadata:
  geometry: "margin=1in"
resource-path:
  - .
  - ${.}/../static
table-of-contents: true
variables:
  toc-title: "Contents"